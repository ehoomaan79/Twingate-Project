[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twingate-client"
version = "0.1.0"
description = "Client endpoint for zero-trust peer and resource access"
requires-python = ">=3.11"
